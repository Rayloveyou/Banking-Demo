# API Producer values - merged with parent values via ArgoCD/Helm
api-producer:
  enabled: true
  replicas: 1
  image:
    repository: datnxdevops/banking-demo-api-producer
    tag: v1
    pullPolicy: Always
  secretRef:
    name: banking-db-secret
    keys:
      databaseUrl: DATABASE_URL
      redisUrl: REDIS_URL
  rabbitmqSecretRef:
    name: rabbitmq-connection-secret
    key: RABBITMQ_URL
  securityContext:
    pod:
      seccompProfile:
        type: RuntimeDefault
    container:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
  service:
    port: 8080
    portName: http
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "256Mi"
      cpu: "300m"
  readinessProbe:
    enabled: true
    path: /health
    port: 8080
    initialDelaySeconds: 10
    periodSeconds: 15
  corsOrigins: http://localhost:3000
  extraEnv: {}
